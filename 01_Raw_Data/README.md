# Raw Data

The project uses a Linux authentication log dataset containing 500,000 records.

The full raw CSV is not included in this repository due to file size considerations.

The dataset contains authentication, account, server, network, and anomaly-related fields.
